import React from "react";
import ReactDOM from "react-dom/client";
import { CanvasStudioApp } from "./CanvasStudioApp";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <CanvasStudioApp />
  </React.StrictMode>
);
